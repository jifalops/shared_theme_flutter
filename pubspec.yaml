name: shared_theme_flutter
description: Flutter plugin to easily share a theme between Flutter and the web.
version: 0.1.2
author: Jacob Phillips <jifalops@gmail.com>
homepage: https://github.com/jifalops/shared_theme_flutter

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  shared_theme: ^0.1.2
  flutter:
    sdk: flutter

dev_dependencies:
  shared_preferences: ^0.4.2
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:

